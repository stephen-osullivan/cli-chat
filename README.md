Chat tool that can be called anywhere in linux with
Has websearch capabilities. Main agent uses Grok.

```$ chat```

Install with:

```$ uv tool install --reinstall .```

Requires XAI_API_KEY and OPENAI_API_KEY to be set.